---
/**
 * CinematicBridge.astro
 * 
 * Production-quality, scroll-driven cinematic camera journey:
 * Hero World -> Approach Monumental Architectural Bridge -> Fly through/underneath
 * -> Dark atmospheric under-bridge portal -> Light emerges -> Seamlessly reveals next section.
 *
 * Built with native Astro, HTML, CSS transforms/perspectives & lightweight RAF scroll listener.
 */
---

<section class="cinematic-portal-section" id="cinematic-portal" aria-label="Cinematic Transition Journey">
  <div class="cinematic-sticky-stage">
    <!-- 3D Camera Frustum World Container -->
    <div class="camera-world" id="camera-world">

      <!-- LAYER 1: Deep Sky, Atmospheric Fog & Majestic Distant Mountains -->
      <div class="world-layer layer-background" id="layer-bg">
        <div class="sky-gradient"></div>
        <div class="horizon-glow"></div>
        
        <!-- Distant Mountain Peaks Layer (Deep Atmospheric Blue/Indigo) -->
        <svg class="distant-mountains-back" viewBox="0 0 1920 600" preserveAspectRatio="none">
          <defs>
            <linearGradient id="mtnBackGrad" x1="0%" y1="0%" x2="0%" y2="100%">
              <stop offset="0%" stop-color="#14284b" stop-opacity="0.9"/>
              <stop offset="60%" stop-color="#0e1d37" stop-opacity="0.95"/>
              <stop offset="100%" stop-color="#081020" stop-opacity="1"/>
            </linearGradient>
          </defs>
          <path d="M0,600 L0,320 L180,210 L340,310 L520,150 L680,260 L850,90 L980,220 L1140,110 L1320,270 L1500,160 L1680,280 L1820,190 L1920,250 L1920,600 Z" fill="url(#mtnBackGrad)"/>
        </svg>

        <!-- Mid-Distance Mountain Ridges with Chiseled Facets & Misty Valleys -->
        <svg class="distant-mountains-front" viewBox="0 0 1920 600" preserveAspectRatio="none">
          <defs>
            <linearGradient id="mtnFrontGrad" x1="0%" y1="0%" x2="0%" y2="100%">
              <stop offset="0%" stop-color="#1a345e" stop-opacity="0.85"/>
              <stop offset="50%" stop-color="#0f203c" stop-opacity="0.95"/>
              <stop offset="100%" stop-color="#070e1a" stop-opacity="1"/>
            </linearGradient>
            <linearGradient id="facetHighlight" x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%" stop-color="#38bdf8" stop-opacity="0.25"/>
              <stop offset="100%" stop-color="#1e3a8a" stop-opacity="0"/>
            </linearGradient>
          </defs>
          <!-- Dark mountain mass -->
          <path d="M0,600 L0,410 L140,330 L280,390 L440,260 L580,340 L720,220 L840,310 L940,190 L1080,320 L1220,230 L1380,350 L1540,240 L1700,340 L1840,270 L1920,330 L1920,600 Z" fill="url(#mtnFrontGrad)"/>
          <!-- Light edge highlights on peak ridges -->
          <path d="M440,260 L510,300 L580,340 L720,220 L780,265 L840,310 L940,190 L1010,255 L1080,320" stroke="url(#facetHighlight)" stroke-width="2" fill="none"/>
          <path d="M1220,230 L1300,290 L1380,350 L1540,240 L1620,290 L1700,340" stroke="url(#facetHighlight)" stroke-width="2" fill="none"/>
        </svg>

        <!-- Distant Architectural Monoliths blending seamlessly into the mountain valleys -->
        <svg class="distant-megastructures" viewBox="0 0 1600 500" preserveAspectRatio="none">
          <path d="M0,500 L0,380 L120,380 L120,320 L160,320 L160,380 L280,380 L310,260 L350,260 L380,380 L520,380 L540,210 L590,210 L610,380 L760,380 L800,180 L840,180 L880,380 L1040,380 L1070,240 L1120,240 L1150,380 L1300,380 L1320,310 L1370,310 L1390,380 L1600,380 L1600,500 Z" fill="#08101e" opacity="0.7"/>
          <path d="M0,500 L0,420 L240,420 L270,340 L330,340 L360,420 L680,420 L710,280 L770,280 L800,420 L1180,420 L1210,320 L1260,320 L1290,420 L1600,420 L1600,500 Z" fill="#050a14" opacity="0.9"/>
        </svg>

        <!-- Valley Fog & Low Atmospheric Mist -->
        <div class="atmospheric-haze"></div>
      </div>

      <!-- LAYER 2: Midground Water Fjord, Shoreline Cliffs & Rock Promontories -->
      <div class="world-layer layer-midground" id="layer-mid">
        <!-- Deep Reflective Water Basin -->
        <div class="water-expanse">
          <div class="water-light-line"></div>
          <div class="water-specular-shimmer"></div>
          <!-- Water Surface Reflections & Ripple Waves -->
          <div class="water-ripples">
            <span class="ripple r-1"></span>
            <span class="ripple r-2"></span>
            <span class="ripple r-3"></span>
            <span class="ripple r-4"></span>
          </div>
        </div>

        <!-- Monumental Rocky Cliffs & Fjord Promontories Flanking the Water -->
        <div class="fjord-cliffs-system">
          <!-- Left Rocky Cliff Complex -->
          <svg class="cliff-svg cliff-left" viewBox="0 0 600 700" preserveAspectRatio="none">
            <defs>
              <linearGradient id="cliffRockGradL" x1="0%" y1="0%" x2="100%" y2="100%">
                <stop offset="0%" stop-color="#142036"/>
                <stop offset="40%" stop-color="#0c1524"/>
                <stop offset="100%" stop-color="#050910"/>
              </linearGradient>
              <linearGradient id="cliffHighlightL" x1="0%" y1="0%" x2="80%" y2="80%">
                <stop offset="0%" stop-color="#38bdf8" stop-opacity="0.3"/>
                <stop offset="100%" stop-color="#0284c7" stop-opacity="0"/>
              </linearGradient>
            </defs>
            <!-- Craggy cliff contours -->
            <polygon points="0,0 240,160 380,290 320,380 460,490 390,570 540,700 0,700" fill="url(#cliffRockGradL)"/>
            <polygon points="0,180 180,260 220,380 340,460 270,550 420,700 0,700" fill="#090f1a" opacity="0.7"/>
            <!-- Chiseled Edge Highlights -->
            <polyline points="0,0 240,160 380,290 320,380 460,490 390,570 540,700" stroke="url(#cliffHighlightL)" stroke-width="2.5" fill="none"/>
            <polyline points="180,260 220,380 340,460" stroke="rgba(255,255,255,0.12)" stroke-width="1.5" fill="none"/>
          </svg>

          <!-- Right Rocky Cliff Complex -->
          <svg class="cliff-svg cliff-right" viewBox="0 0 600 700" preserveAspectRatio="none">
            <defs>
              <linearGradient id="cliffRockGradR" x1="100%" y1="0%" x2="0%" y2="100%">
                <stop offset="0%" stop-color="#142036"/>
                <stop offset="40%" stop-color="#0c1524"/>
                <stop offset="100%" stop-color="#050910"/>
              </linearGradient>
              <linearGradient id="cliffHighlightR" x1="100%" y1="0%" x2="20%" y2="80%">
                <stop offset="0%" stop-color="#38bdf8" stop-opacity="0.3"/>
                <stop offset="100%" stop-color="#0284c7" stop-opacity="0"/>
              </linearGradient>
            </defs>
            <!-- Craggy cliff contours -->
            <polygon points="600,0 360,160 220,290 280,380 140,490 210,570 60,700 600,700" fill="url(#cliffRockGradR)"/>
            <polygon points="600,180 420,260 380,380 260,460 330,550 180,700 600,700" fill="#090f1a" opacity="0.7"/>
            <!-- Chiseled Edge Highlights -->
            <polyline points="600,0 360,160 220,290 280,380 140,490 210,570 60,700" stroke="url(#cliffHighlightR)" stroke-width="2.5" fill="none"/>
            <polyline points="420,260 380,380 260,460" stroke="rgba(255,255,255,0.12)" stroke-width="1.5" fill="none"/>
          </svg>
        </div>
      </div>

      <!-- LAYER 3: MONUMENTAL ARCHITECTURAL BRIDGE (The Hero Structure) -->
      <!-- Positioned in true 3D perspective to zoom, tower, and pass overhead -->
      <div class="world-layer layer-bridge-system" id="layer-bridge">
        <div class="bridge-3d-rig" id="bridge-rig">

          <!-- Architectural Suspension Arches & Crown Cables -->
          <div class="bridge-suspension-arches">
            <svg class="arch-cable-svg" viewBox="0 0 1400 300" preserveAspectRatio="none">
              <!-- Left catenary parabolic suspension cable -->
              <path d="M0,280 Q320,60 620,280" fill="none" stroke="rgba(56, 189, 248, 0.55)" stroke-width="3" filter="drop-shadow(0 0 8px rgba(56,189,248,0.4))"/>
              <!-- Right catenary parabolic suspension cable -->
              <path d="M780,280 Q1080,60 1400,280" fill="none" stroke="rgba(56, 189, 248, 0.55)" stroke-width="3" filter="drop-shadow(0 0 8px rgba(56,189,248,0.4))"/>
              <!-- Center sweeping tension arch bridging the gap -->
              <path d="M300,280 Q700,-20 1100,280" fill="none" stroke="rgba(37, 99, 235, 0.45)" stroke-width="2"/>
            </svg>
          </div>

          <!-- Bridge Deck & Superstructure (Upper Roadway, Truss & Ribs) -->
          <div class="bridge-superstructure">
            <div class="superstructure-line top-spine"></div>
            <!-- High density architectural stay-cable fan array -->
            <div class="truss-cables-row">
              <span class="cable c1"></span>
              <span class="cable c2"></span>
              <span class="cable c3"></span>
              <span class="cable c4"></span>
              <span class="cable c5"></span>
              <span class="cable c6"></span>
              <span class="cable c7"></span>
              <span class="cable c8"></span>
              <span class="cable c9"></span>
              <span class="cable c10"></span>
              <span class="cable c11"></span>
              <span class="cable c12"></span>
            </div>
            <div class="deck-main-beam">
              <div class="deck-light-strip"></div>
              <div class="deck-luminescent-edge"></div>
            </div>
          </div>

          <!-- Massive Architectural Pylon Towers (Left and Right Piers) -->
          <div class="monolithic-pylon pylon-left">
            <div class="pylon-shaft"></div>
            <div class="pylon-accent"></div>
            <div class="pylon-bracket top-br"></div>
            <div class="pylon-bracket bot-br"></div>
          </div>
          <div class="monolithic-pylon pylon-right">
            <div class="pylon-shaft"></div>
            <div class="pylon-accent"></div>
            <div class="pylon-bracket top-br"></div>
            <div class="pylon-bracket bot-br"></div>
          </div>

          <!-- Bridge Foundation Piers rooted in the deep water -->
          <div class="substructure-piers">
            <div class="water-pier pier-left">
              <div class="pier-reflection"></div>
            </div>
            <div class="water-pier pier-right">
              <div class="pier-reflection"></div>
            </div>
          </div>

          <!-- Bridge Underside (Covers viewport as camera passes underneath) -->
          <div class="bridge-undercarriage" id="bridge-undercarriage">
            <div class="ribbed-soffit">
              <div class="transverse-rib r1"></div>
              <div class="transverse-rib r2"></div>
              <div class="transverse-rib r3"></div>
              <div class="transverse-rib r4"></div>
              <div class="transverse-rib r5"></div>
              <div class="transverse-rib r6"></div>
              <div class="transverse-rib r7"></div>
              <div class="transverse-rib r8"></div>
              <div class="transverse-rib r9"></div>
              <div class="transverse-rib r10"></div>
            </div>
            <!-- Overhead longitudinal expansion joints with soft glowing cyan conduits -->
            <div class="soffit-conduits">
              <span class="conduit c-left"></span>
              <span class="conduit c-center"></span>
              <span class="conduit c-right"></span>
            </div>
          </div>

        </div>
      </div>

      <!-- LAYER 4: The Portal Dark Tunnel & Exit Light Emergence -->
      <div class="world-layer layer-portal" id="layer-portal">
        <div class="tunnel-occlusion" id="tunnel-occlusion"></div>
        <div class="exit-light-source" id="exit-light">
          <div class="exit-radiant-core"></div>
          <div class="exit-volumetric-flare"></div>
        </div>
      </div>

      <!-- LAYER 5: Foreground & Extreme Depth Silhouettes -->
      <div class="world-layer layer-foreground" id="layer-fore">
        <div class="architectural-framing-edge left-fin" id="fore-left"></div>
        <div class="architectural-framing-edge right-fin" id="fore-right"></div>
      </div>

      <!-- LAYER 6: Revealed New Environment (Seamlessly matches #pillars start) -->
      <div class="world-layer layer-revealed-world" id="layer-revealed">
        <div class="revealed-ambient-glow"></div>
        <div class="revealed-matrix-grid"></div>
      </div>

      <!-- CINEMATIC STORYTELLING TYPOGRAPHY STAGES -->
      <div class="story-typography-viewport">
        <!-- Frame 1 (Approach) -->
        <div class="story-caption" id="caption-1">
          <span class="caption-kicker">01 / GENESIS</span>
          <h2 class="caption-title">Every scalable idea starts somewhere.</h2>
        </div>

        <!-- Frame 2 (Dominance / Monumental Bridge) -->
        <div class="story-caption" id="caption-2">
          <span class="caption-kicker">02 / MOMENTUM</span>
          <h2 class="caption-title">Then engineering gives it structure.</h2>
        </div>

        <!-- Frame 3 (Underbelly Passage) -->
        <div class="story-caption" id="caption-3">
          <span class="caption-kicker">03 / TRANSITION</span>
          <h2 class="caption-title">Passing through the crucible into reality.</h2>
        </div>

        <!-- Frame 4 (Light Emergence & Gateway to Pillars) -->
        <div class="story-caption" id="caption-4">
          <span class="caption-kicker">04 / THE MATRIX</span>
          <h2 class="caption-title">Let’s build what comes next.</h2>
        </div>
      </div>

    </div>
  </div>
</section>

<script>
  function initCinematicScroll() {
    const portal = document.getElementById('cinematic-portal');
    const bridgeRig = document.getElementById('bridge-rig');
    const undercarriage = document.getElementById('bridge-undercarriage');
    const tunnelOcclusion = document.getElementById('tunnel-occlusion');
    const exitLight = document.getElementById('exit-light');
    const layerBg = document.getElementById('layer-bg');
    const layerMid = document.getElementById('layer-mid');
    const foreLeft = document.getElementById('fore-left');
    const foreRight = document.getElementById('fore-right');
    const layerRevealed = document.getElementById('layer-revealed');

    const cap1 = document.getElementById('caption-1');
    const cap2 = document.getElementById('caption-2');
    const cap3 = document.getElementById('caption-3');
    const cap4 = document.getElementById('caption-4');

    if (!portal || !bridgeRig) return;

    // Smooth easing function for cinematic deceleration
    function easeInOutQuad(t: number) {
      return t < 0.5 ? 2 * t * t : -1 + (4 - 2 * t) * t;
    }

    function updateFrame() {
      const rect = portal.getBoundingClientRect();
      const windowHeight = window.innerHeight;
      const totalScrollable = rect.height - windowHeight;
      if (totalScrollable <= 0) return;

      const currentScroll = -rect.top;
      let p = currentScroll / totalScrollable;
      p = Math.max(0, Math.min(1, p));

      // -------------------------------------------------------------
      // 1. 3D CAMERA TRACKING SHOT (Forward Zoom & Passage)
      // 0.0 -> 0.65: Approach & Towering Scale
      // 0.65 -> 0.85: Passing Underneath Bridge & Overhead Soffit Occlusion
      // 0.85 -> 1.0: Emergence into Next Realm
      // -------------------------------------------------------------
      
      // Camera forward position: scale from 0.7 (distance) to 12.0 (overhead flythrough)
      let scaleVal: number;
      let translateYVal: number;
      let translateZVal: number;

      if (p < 0.65) {
        // Approaching phase
        const norm = p / 0.65;
        const eased = easeInOutQuad(norm);
        scaleVal = 0.7 + eased * 2.8; // 0.7 -> 3.5
        translateYVal = eased * 120;   // bridge sinks slightly as camera elevates
        translateZVal = eased * 400;
      } else {
        // Passing underneath & overhead phase
        const norm = (p - 0.65) / 0.35;
        scaleVal = 3.5 + norm * 8.5;  // 3.5 -> 12.0 (massive overhead engulfment)
        translateYVal = 120 - norm * 380; // fly completely underneath and up
        translateZVal = 400 + norm * 1200;
      }

      bridgeRig.style.transform = `translate3d(0px, ${translateYVal}px, ${translateZVal}px) scale(${scaleVal})`;

      // -------------------------------------------------------------
      // 2. PARALLAX DEPTH LAYERS
      // -------------------------------------------------------------
      if (layerBg) {
        layerBg.style.transform = `scale(${1 + p * 0.15}) translateY(${p * -40}px)`;
      }
      if (layerMid) {
        layerMid.style.transform = `scale(${1 + p * 0.4}) translateY(${p * 20}px)`;
      }
      if (foreLeft && foreRight) {
        // Foreground architectural fins part sideways as camera pushes past
        const forePush = Math.min(1, p / 0.4);
        foreLeft.style.transform = `translateX(${forePush * -140}px) scale(${1 + forePush * 0.3})`;
        foreRight.style.transform = `translateX(${forePush * 140}px) scale(${1 + forePush * 0.3})`;
        foreLeft.style.opacity = `${1 - p * 1.8}`;
        foreRight.style.opacity = `${1 - p * 1.8}`;
      }

      // -------------------------------------------------------------
      // 3. UNDER-BRIDGE OCCLUSION & TUNNEL DARKNESS (p: 0.60 -> 0.88)
      // -------------------------------------------------------------
      if (tunnelOcclusion) {
        if (p < 0.55) {
          tunnelOcclusion.style.opacity = '0';
        } else if (p >= 0.55 && p <= 0.78) {
          // Darken environment as we enter undercarriage
          const darkNorm = (p - 0.55) / 0.23;
          tunnelOcclusion.style.opacity = `${darkNorm * 0.95}`;
        } else {
          // Exit darkness into emergence light
          const lightNorm = (p - 0.78) / 0.22;
          tunnelOcclusion.style.opacity = `${Math.max(0, 0.95 - lightNorm * 1.4)}`;
        }
      }

      // Undercarriage overhead ceiling visibility
      if (undercarriage) {
        if (p > 0.45 && p < 0.90) {
          undercarriage.style.opacity = '1';
        } else {
          undercarriage.style.opacity = '0.1';
        }
      }

      // -------------------------------------------------------------
      // 4. EXIT LIGHT EMERGENCE (p: 0.75 -> 1.0)
      // -------------------------------------------------------------
      if (exitLight) {
        if (p < 0.72) {
          exitLight.style.opacity = '0';
          exitLight.style.transform = 'translate(-50%, -50%) scale(0.3)';
        } else {
          const exitP = (p - 0.72) / 0.28;
          exitLight.style.opacity = `${Math.min(1, exitP * 1.6)}`;
          exitLight.style.transform = `translate(-50%, -50%) scale(${0.4 + exitP * 4.5})`;
        }
      }

      // -------------------------------------------------------------
      // 5. REVEALED NEW ENVIRONMENT (Final transition seamlessly into Pillars)
      // -------------------------------------------------------------
      if (layerRevealed) {
        if (p < 0.82) {
          layerRevealed.style.opacity = '0';
        } else {
          const revP = (p - 0.82) / 0.18;
          layerRevealed.style.opacity = `${revP}`;
        }
      }

      // -------------------------------------------------------------
      // 6. CINEMATIC STORYTELLING TEXT PHASING
      // -------------------------------------------------------------
      function calcCaption(targetP: number, spread: number) {
        const dist = Math.abs(p - targetP);
        if (dist > spread) return 0;
        return Math.cos((dist / spread) * (Math.PI / 2));
      }

      if (cap1) {
        const o1 = calcCaption(0.12, 0.12);
        cap1.style.opacity = `${o1}`;
        cap1.style.transform = `translateY(${(1 - o1) * 20}px)`;
      }
      if (cap2) {
        const o2 = calcCaption(0.38, 0.13);
        cap2.style.opacity = `${o2}`;
        cap2.style.transform = `translateY(${(1 - o2) * 20}px)`;
      }
      if (cap3) {
        const o3 = calcCaption(0.68, 0.12);
        cap3.style.opacity = `${o3}`;
        cap3.style.transform = `translateY(${(1 - o3) * 20}px)`;
      }
      if (cap4) {
        const o4 = calcCaption(0.92, 0.10);
        cap4.style.opacity = `${o4}`;
        cap4.style.transform = `translateY(${(1 - o4) * 20}px)`;
      }
    }

    window.addEventListener('scroll', updateFrame, { passive: true });
    updateFrame();
  }

  // Initialize immediately & on client page load
  initCinematicScroll();
  document.addEventListener('astro:page-load', initCinematicScroll);
</script>

<style>
  /* =========================================================================
     CINEMATIC SCROLLING SECTION FOUNDATION
     Height: 450vh allows unhurried, monumental tracking shot pacing
     ========================================================================= */
  .cinematic-portal-section {
    position: relative;
    height: 450vh;
    background-color: #070d18;
    margin: 0;
    padding: 0;
    border: none;
    overflow: clip;
  }

  .cinematic-sticky-stage {
    position: sticky;
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
    overflow: hidden;
    perspective: 1200px;
    perspective-origin: 50% 55%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #060b14;
  }

  /* 3D Viewport Frustum */
  .camera-world {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    transform-style: preserve-3d;
    pointer-events: none;
    overflow: hidden;
  }

  .world-layer {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
  }

  /* -------------------------------------------------------------------------
     LAYER 1: Background Sky, Atmosphere, Mountain Ranges & Megastructures
     ------------------------------------------------------------------------- */
  .layer-background {
    z-index: 1;
    transform-origin: 50% 50%;
    will-change: transform;
  }

  .sky-gradient {
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at 50% 48%, #0d1e38 0%, #081224 45%, #050a14 100%);
  }

  .horizon-glow {
    position: absolute;
    top: 48%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 80vw;
    height: 140px;
    background: radial-gradient(ellipse at center, rgba(6, 182, 212, 0.28) 0%, rgba(37, 99, 235, 0.12) 55%, transparent 75%);
    filter: blur(45px);
  }

  /* Alpine Peaks Layer (Deep Atmospheric Silhouette) */
  .distant-mountains-back {
    position: absolute;
    bottom: 30%;
    left: 0;
    width: 100%;
    height: 38vh;
    opacity: 0.85;
    filter: blur(1.2px);
  }

  /* Foreground Mountain Ridges with Light Highlights */
  .distant-mountains-front {
    position: absolute;
    bottom: 25%;
    left: 0;
    width: 100%;
    height: 32vh;
    opacity: 0.95;
    filter: blur(0.4px);
  }

  .distant-megastructures {
    position: absolute;
    bottom: 26%;
    left: 0;
    width: 100%;
    height: 24vh;
    opacity: 0.6;
    filter: blur(1.8px);
  }

  .atmospheric-haze {
    position: absolute;
    top: 44%;
    left: 0;
    right: 0;
    height: 28vh;
    background: linear-gradient(180deg, transparent 0%, rgba(8, 18, 36, 0.75) 45%, rgba(6, 11, 20, 0.96) 100%);
  }

  /* -------------------------------------------------------------------------
     LAYER 2: Midground Water Fjord Basin, Shoreline Cliffs & Ripple Reflections
     ------------------------------------------------------------------------- */
  .layer-midground {
    z-index: 2;
    transform-origin: 50% 60%;
    will-change: transform;
  }

  .water-expanse {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 42vh;
    background: linear-gradient(180deg, #061122 0%, #030914 45%, #01040a 100%);
    overflow: hidden;
  }

  .water-light-line {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 70vw;
    height: 2px;
    background: linear-gradient(90deg, transparent, rgba(6, 182, 212, 0.7), rgba(56, 189, 248, 0.85), transparent);
    box-shadow: 0 0 20px rgba(6, 182, 212, 0.6);
  }

  .water-specular-shimmer {
    position: absolute;
    top: 0;
    left: 30%;
    right: 30%;
    height: 35px;
    background: radial-gradient(ellipse at top, rgba(6, 182, 212, 0.25) 0%, transparent 80%);
    filter: blur(8px);
    pointer-events: none;
  }

  .water-ripples {
    position: absolute;
    inset: 0;
    opacity: 0.55;
    pointer-events: none;
  }

  .water-ripples .ripple {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    border-radius: 50%;
    border-top: 1px solid rgba(56, 189, 248, 0.3);
    border-bottom: 1px solid transparent;
  }

  .ripple.r-1 { top: 12%; width: 55vw; height: 18px; }
  .ripple.r-2 { top: 32%; width: 75vw; height: 26px; border-top-color: rgba(6, 182, 212, 0.22); }
  .ripple.r-3 { top: 56%; width: 90vw; height: 36px; border-top-color: rgba(37, 99, 235, 0.18); }
  .ripple.r-4 { top: 78%; width: 110vw; height: 48px; border-top-color: rgba(255, 255, 255, 0.08); }

  /* Rocky Fjord Cliff Complex */
  .fjord-cliffs-system {
    position: absolute;
    bottom: 0;
    inset-inline: 0;
    height: 52vh;
    pointer-events: none;
  }

  .cliff-svg {
    position: absolute;
    bottom: 0;
    height: 100%;
    width: 38vw;
    filter: drop-shadow(0 15px 30px rgba(0,0,0,0.9));
  }

  .cliff-left {
    left: -4vw;
  }

  .cliff-right {
    right: -4vw;
  }

  /* -------------------------------------------------------------------------
     LAYER 3: MONUMENTAL ARCHITECTURAL BRIDGE SYSTEM
     ------------------------------------------------------------------------- */
  .layer-bridge-system {
    z-index: 4;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .bridge-3d-rig {
    position: absolute;
    top: 46%;
    left: 50%;
    width: 90vw;
    max-width: 1400px;
    height: 48vh;
    margin-left: calc(-45vw);
    transform-origin: 50% 60%;
    transform-style: preserve-3d;
    will-change: transform;
    pointer-events: none;
  }

  @media (min-width: 1400px) {
    .bridge-3d-rig {
      margin-left: -700px;
      width: 1400px;
    }
  }

  /* Suspension Arches System */
  .bridge-suspension-arches {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 35%;
    pointer-events: none;
    z-index: 2;
  }

  .arch-cable-svg {
    width: 100%;
    height: 100%;
    overflow: visible;
  }

  /* Superstructure (Main beam & high-tension cable matrix) */
  .bridge-superstructure {
    position: absolute;
    top: 22%;
    left: 0;
    right: 0;
    height: 32%;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    z-index: 3;
  }

  .deck-main-beam {
    width: 100%;
    height: 26px;
    background: linear-gradient(180deg, #1e2f47 0%, #0d1726 100%);
    border-top: 2px solid #38bdf8;
    border-bottom: 2px solid #08101c;
    box-shadow: 0 14px 40px rgba(0, 0, 0, 0.9), 0 0 30px rgba(56, 189, 248, 0.25);
    position: relative;
  }

  .deck-light-strip {
    position: absolute;
    top: -2px;
    left: 8%;
    right: 8%;
    height: 2px;
    background: linear-gradient(90deg, transparent, #06b6d4, #2563eb, #38bdf8, transparent);
  }

  .deck-luminescent-edge {
    position: absolute;
    bottom: -2px;
    left: 15%;
    right: 15%;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(6, 182, 212, 0.4), transparent);
  }

  .truss-cables-row {
    position: absolute;
    top: 0;
    left: 10%;
    right: 10%;
    bottom: 26px;
    display: flex;
    justify-content: space-between;
    opacity: 0.7;
  }

  .truss-cables-row .cable {
    width: 1px;
    height: 100%;
    background: linear-gradient(180deg, transparent 0%, rgba(255, 255, 255, 0.5) 100%);
  }

  .cable.c1, .cable.c12 { transform: rotate(26deg); }
  .cable.c2, .cable.c11 { transform: rotate(20deg); }
  .cable.c3, .cable.c10 { transform: rotate(14deg); }
  .cable.c4, .cable.c9  { transform: rotate(8deg); }
  .cable.c5, .cable.c8  { transform: rotate(4deg); }
  .cable.c6, .cable.c7  { transform: rotate(0deg); }

  /* Monolithic Architectural Pylon Piers */
  .monolithic-pylon {
    position: absolute;
    bottom: 0;
    top: 5%;
    width: 36px;
    background: linear-gradient(90deg, #132033 0%, #223756 40%, #0d1522 100%);
    box-shadow: 0 0 35px rgba(0, 0, 0, 0.95);
    z-index: 4;
  }

  .pylon-left {
    left: 22%;
    clip-path: polygon(25% 0, 75% 0, 100% 100%, 0% 100%);
  }

  .pylon-right {
    right: 22%;
    clip-path: polygon(25% 0, 75% 0, 100% 100%, 0% 100%);
  }

  .pylon-accent {
    position: absolute;
    top: 0;
    bottom: 0;
    left: 50%;
    width: 2px;
    background: rgba(6, 182, 212, 0.7);
    box-shadow: 0 0 8px rgba(6, 182, 212, 0.5);
  }

  .pylon-bracket {
    position: absolute;
    left: -12px;
    right: -12px;
    height: 4px;
    background: #2563eb;
    box-shadow: 0 0 10px rgba(37, 99, 235, 0.6);
  }
  .pylon-bracket.top-br { top: 35%; }
  .pylon-bracket.bot-br { top: 65%; }

  /* Substructure Piers in Water */
  .substructure-piers {
    position: absolute;
    bottom: -15%;
    left: 0;
    right: 0;
    height: 25%;
    pointer-events: none;
    z-index: 1;
  }

  .water-pier {
    position: absolute;
    bottom: 0;
    width: 44px;
    height: 100%;
    background: linear-gradient(180deg, #0f1c2e 0%, #070d17 100%);
    box-shadow: 0 10px 20px rgba(0,0,0,0.9);
  }
  .pier-left { left: calc(22% - 4px); }
  .pier-right { right: calc(22% - 4px); }

  .pier-reflection {
    position: absolute;
    top: 100%;
    left: 0;
    width: 100%;
    height: 30px;
    background: linear-gradient(180deg, rgba(6, 182, 212, 0.25) 0%, transparent 100%);
    filter: blur(4px);
  }

  /* Bridge Undercarriage (Passed directly overhead) */
  .bridge-undercarriage {
    position: absolute;
    top: 48%;
    left: 5%;
    right: 5%;
    height: 140px;
    background: #091220;
    transform: rotateX(75deg);
    transform-origin: 50% 0%;
    border-left: 2px solid #1e293b;
    border-right: 2px solid #1e293b;
    border-bottom: 2px solid #0f172a;
    box-shadow: inset 0 0 60px #000000;
    opacity: 0.1;
    transition: opacity 0.2s ease-out;
  }

  .ribbed-soffit {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    justify-content: space-evenly;
  }

  .transverse-rib {
    width: 100%;
    height: 6px;
    background: #132238;
    border-top: 1px solid rgba(255, 255, 255, 0.12);
    border-bottom: 1px solid #050a12;
  }

  .soffit-conduits {
    position: absolute;
    inset: 0;
    display: flex;
    justify-content: space-evenly;
  }

  .conduit {
    width: 2px;
    height: 100%;
    background: rgba(6, 182, 212, 0.4);
    box-shadow: 0 0 10px rgba(6, 182, 212, 0.3);
  }

  /* -------------------------------------------------------------------------
     LAYER 4: Portal Darkness & Exit Light Emergence
     ------------------------------------------------------------------------- */
  .layer-portal {
    z-index: 6;
  }

  .tunnel-occlusion {
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at 50% 50%, rgba(4, 7, 14, 0.98) 0%, #03060c 100%);
    opacity: 0;
    pointer-events: none;
    will-change: opacity;
  }

  .exit-light-source {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%) scale(0.5);
    width: 280px;
    height: 280px;
    opacity: 0;
    pointer-events: none;
    will-change: transform, opacity;
  }

  .exit-radiant-core {
    position: absolute;
    inset: 0;
    border-radius: 50%;
    background: radial-gradient(circle, #ffffff 0%, rgba(56, 189, 248, 0.9) 30%, rgba(37, 99, 235, 0.5) 60%, transparent 80%);
    filter: blur(18px);
  }

  .exit-volumetric-flare {
    position: absolute;
    top: 50%;
    left: -150%;
    right: -150%;
    height: 3px;
    background: linear-gradient(90deg, transparent 0%, rgba(6, 182, 212, 0.8) 40%, #ffffff 50%, rgba(6, 182, 212, 0.8) 60%, transparent 100%);
    transform: translateY(-50%);
    filter: blur(1px);
  }

  /* -------------------------------------------------------------------------
     LAYER 5: Foreground Architectural Framing Edges
     ------------------------------------------------------------------------- */
  .layer-foreground {
    z-index: 7;
  }

  .architectural-framing-edge {
    position: absolute;
    bottom: -10vh;
    width: 14vw;
    height: 65vh;
    background: linear-gradient(180deg, #091322 0%, #040810 100%);
    border: 1px solid rgba(255, 255, 255, 0.06);
    filter: blur(4px); /* extreme foreground cinematic depth blur */
    will-change: transform, opacity;
  }

  .left-fin {
    left: -2vw;
    clip-path: polygon(0 0, 100% 40%, 40% 100%, 0% 100%);
  }

  .right-fin {
    right: -2vw;
    clip-path: polygon(0 40%, 100% 0, 100% 100%, 60% 100%);
  }

  /* -------------------------------------------------------------------------
     LAYER 6: Revealed New Environment (Prepares seamless entry to Pillars)
     ------------------------------------------------------------------------- */
  .layer-revealed-world {
    z-index: 8;
    background: #0f172a;
    opacity: 0;
    will-change: opacity;
  }

  .revealed-ambient-glow {
    position: absolute;
    top: 35%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 65vw;
    height: 350px;
    background: radial-gradient(circle, rgba(37, 99, 235, 0.25) 0%, rgba(6, 182, 212, 0.15) 50%, transparent 75%);
    filter: blur(70px);
  }

  .revealed-matrix-grid {
    position: absolute;
    inset: 0;
    background-image: radial-gradient(rgba(255, 255, 255, 0.08) 1px, transparent 1px);
    background-size: 32px 32px;
  }

  /* -------------------------------------------------------------------------
     STORYTELLING MINIMALIST TYPOGRAPHY (Quiet, elegant, non-intrusive)
     ------------------------------------------------------------------------- */
  .story-typography-viewport {
    position: absolute;
    inset: 0;
    z-index: 10;
    display: flex;
    align-items: center;
    justify-content: center;
    pointer-events: none;
  }

  .story-caption {
    position: absolute;
    text-align: center;
    max-width: 820px;
    padding: 0 var(--space-6);
    opacity: 0;
    will-change: opacity, transform;
    pointer-events: none;
  }

  .caption-kicker {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: var(--font-size-caption);
    letter-spacing: var(--letter-spacing-wider);
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: var(--space-2);
    background: rgba(15, 23, 42, 0.7);
    padding: 0.25rem 0.75rem;
    border-radius: var(--radius-full);
    border: 1px solid rgba(6, 182, 212, 0.25);
    backdrop-filter: blur(8px);
  }

  .caption-title {
    font-size: clamp(2rem, 4.5vw, 3.75rem);
    font-weight: var(--font-weight-extrabold);
    color: var(--raw-white);
    line-height: 1.15;
    letter-spacing: var(--letter-spacing-tight);
    text-shadow: 0 4px 30px rgba(0, 0, 0, 0.95);
  }

  /* -------------------------------------------------------------------------
     ACCESSIBILITY & REDUCED MOTION
     ------------------------------------------------------------------------- */
  @media (prefers-reduced-motion: reduce) {
    .cinematic-portal-section {
      height: auto;
    }

    .cinematic-sticky-stage {
      position: relative;
      height: 70vh;
      perspective: none;
    }

    .bridge-3d-rig {
      transform: none !important;
      scale: 1 !important;
    }

    .story-caption {
      position: relative;
      opacity: 1 !important;
      transform: none !important;
    }

    #caption-2, #caption-3, #caption-4 {
      display: none;
    }
  }

  /* -------------------------------------------------------------------------
     MOBILE VIEWPORT OPTIMIZATION
     ------------------------------------------------------------------------- */
  @media (max-width: 768px) {
    .cinematic-portal-section {
      height: 320vh; /* Shorter scroll duration on touchscreens */
    }

    .bridge-3d-rig {
      width: 95vw;
      margin-left: -47.5vw;
    }

    .distant-megastructures {
      display: none; /* Reduce complexity on mobile */
    }

    .architectural-framing-edge {
      display: none;
    }

    .caption-title {
      font-size: 2rem;
    }
  }
</style>
